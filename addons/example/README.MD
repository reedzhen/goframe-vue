### 生成 ctrl

```shell
gf gen ctrl --srcFolder addons/example/api --dstFolder addons/example/controller 
```

### 生成 service

```shell
gf gen service -s=addons/example/logic -d=addons/example/service
```

